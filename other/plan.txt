name: Deploy Demo Environment
on:
  workflow_dispatch:  # Manual trigger

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy infrastructure
        run: terraform apply -auto-approve
      
      - name: Deploy app
        run: kubectl apply -f k8s/
      
      - name: Comment with URL
        run: echo "Demo ready at https://demo-${{ github.run_id }}.yoursite.com"
      
      - name: Schedule destroy
        run: echo "Will auto-destroy in 4 hours"
```

**Costs: ~$10/month** (only when demo is running)

---

### **Option 3: Video demo (free)**

**Record 3-minute video:**
1. Show Grafana dashboard with metrics
2. Run load test, watch pods scale
3. Show Terraform apply creating infrastructure
4. Show CI/CD pipeline deploying

**Upload to YouTube, embed in README.**

**Cost: $0**

---

## WHAT TO ACTUALLY BUILD (PRIORITY ORDER)

### **Week 1: Infrastructure Foundation**

**Goal: Deploy your existing app to Kubernetes on AWS**

**Deliverables:**
1. Terraform that creates:
   - VPC with public/private subnets
   - EKS cluster
   - RDS Postgres (or Postgres in K8s to save costs)
   - Load balancer
2. Kubernetes manifests:
   - Frontend deployment
   - Backend deployment  
   - Services, Ingress
   - Resource limits, probes
3. One-command deployment script
4. Basic documentation

**Public repo structure:**
```
infrastructure/
├── terraform/
│   ├── main.tf
│   ├── vpc.tf
│   ├── eks.tf
│   └── outputs.tf
├── kubernetes/
│   ├── frontend/
│   ├── backend/
│   └── kustomization.yaml
└── README.md
```

---

### **Week 2: Monitoring & Observability**

**Goal: Add production-grade monitoring**

**Deliverables:**
1. Prometheus + Grafana deployed via Helm
2. Custom dashboards:
   - System overview
   - Application metrics
   - Database metrics
3. Alert rules for critical issues
4. Screenshots of dashboards
5. Instrumentation in your app (`/metrics` endpoints)

**Add to repo:**
```
monitoring/
├── prometheus/
│   ├── values.yaml
│   └── alerts.yaml
├── grafana/
│   └── dashboards/
│       ├── overview.json
│       ├── api-metrics.json
│       └── database.json
└── README.md
```

---

### **Week 3: CI/CD Pipeline**

**Goal: Automated deployment**

**Deliverables:**
1. GitHub Actions workflow:
   - Build Docker images
   - Push to registry
   - Deploy to K8s
   - Run smoke tests
2. Multi-environment support (dev/prod)
3. Automated rollback
4. Pipeline visible in Actions tab

**Add to repo:**
```
.github/
└── workflows/
    ├── build-and-deploy.yml
    ├── destroy-demo.yml
    └── load-test.yml
```

---

### **Week 4: Load Testing & Documentation**

**Goal: Prove it works at scale**

**Deliverables:**
1. Load test scenarios (k6 or Locust)
2. Run tests, capture results
3. Screenshots showing:
   - HPA scaling pods
   - Latency staying low under load
   - Zero errors
4. Comprehensive documentation
5. Architecture diagrams
6. Demo video (optional)

**Add to repo:**
```
load-tests/
├── scenarios/
│   ├── normal-load.js
│   └── spike-traffic.js
├── results/
│   ├── screenshots/
│   └── metrics.md
└── README.md

docs/
├── ARCHITECTURE.md
├── DEPLOYMENT.md
└── MONITORING.md

screenshots/
├── grafana-overview.png
├── scaling-in-action.png
└── load-test-results.png
```

---

## FINAL PUBLIC REPO CHECKLIST

**Before sharing with recruiters:**

### **Must-haves:**
- ✅ Professional README with metrics
- ✅ Architecture diagram
- ✅ Clean, documented code
- ✅ Working deployment (even if you destroy it nightly)
- ✅ Monitoring screenshots
- ✅ Load test results
- ✅ CI/CD pipeline visible

### **Nice-to-haves:**
- ✅ Live demo URL (or video)
- ✅ Blog post explaining the build
- ✅ Contribution history showing iterative development
- ✅ Issues/PRs showing problem-solving

### **Must-not-haves:**
- ❌ Secrets / credentials
- ❌ Real client data
- ❌ Production IPs / endpoints
- ❌ Sloppy code or TODO comments
- ❌ Broken links

---

## YOUR ACTION PLAN (STARTING NOW)

### **Today (2-3 hours):**
1. Create `platform-showcase` repo (public)
2. Copy your existing app code into `/app`
3. Create folder structure:
```
   platform-showcase/
   ├── app/
   ├── infrastructure/
   ├── monitoring/
   ├── .github/workflows/
   └── README.md